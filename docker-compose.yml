version: '3'
services:
  app:
    container_name: lista_de_compras
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 9000:80
      - 5173:5173
    volumes:
      - .:/var/www/html/lista_de_compras
    #network_mode: "host"
    environment:
      - UID=${UID}
      - GID=${GID}
    

